# 掉落物设计

> 与 EnemyDesign.md 同级的策划设计稿；实现落地时在 doc/ 新建对应文档并登记 OVERVIEW.md。
> 怪物汇总投放表（属性等级/积分/掉落/描述）：`CPPRogue/Assets/Resources/EnemyTable.json`
> （游戏内 Resources 加载；代码镜像与同步测试见 [doc/CODEX.md](doc/CODEX.md)）。

## 0. 两条线：语法块 vs 材料

| | 语法块 Fragment | 升级材料 |
|---|---|---|
| 定位 | 局内构筑素材，捡到即用，是 Build 本体 | 局外成长货币，撤离结算后生效 |
| 掉落 | 全部敌人，按稀有度（IDEA.md §5） | 普通/精英小额，Boss 大额 |
| 平衡阀门 | CPU 周期（越强的块越贵） | 兑换所需数量递增 |

原则：**材料永不进 Build，Fragment 永不做局外消费**——双重身份会把拾取决策变成两难算术。

三件套（命名定稿 2026-08-27）：

| 槽位 | 名字 | 效果 |
|---|---|---|
| 时间复杂度 | **时间片** Time Slice | 永久延长单次 tick 的执行时间窗 |
| 空间复杂度 | **RAM** | 永久 +Routine 行数上限 |
| 技能树 | **驱动块** Driver | 技能树货币（技能树未设计，先记账） |

三个都**按数量计算**：纯整数计数，无品质/档位之分；合成升档只发生在代码块上（§3）。

## 1. 时间片：执行时间窗与超时惩罚

- **执行时间窗**（驱动层概念）：单次 tick 调用允许占用的最大墙钟时间。基础 = 1×TickInterval（3s），时间片兑换永久 +1s/级（锚点：首级 5 片，待实测）。基础窗口刻意只够中等 Build 跑完——重程序要么精简，要么投资时间片，这正是"时间复杂度"的成长意义。
- **超时惩罚（三段式，定稿）**：
  1. **窗内**：正常执行，无惩罚。
  2. **超窗 → 3s 宽限期**：程序继续执行；玩家移速在 3s 内**逐渐降低至 0**；期间**每秒受到最大生命值×10% 伤害**（宽限走满共 30%）。宽限内跑完 → 惩罚即止、移速恢复，本 tick 正常收尾。
  3. **宽限 3s 仍未跑完 → 立即眩晕 3s**：本次执行强杀（kill -9）；眩晕期间**不触发任何代码块**（tick 挂起），**怪物伤害照常结算**。眩晕结束恢复正常节拍。
- **为什么能防数值爆炸**：死循环/超重代码在宽限内拿不完收益，还要吃减速+掉血，超时直接瘫痪 3s 站着挨打——堆时间换输出不可行；逻辑层计数安全阀（MaxLoopIterations / MaxStatementsPerTick）原样保留，墙钟窗与计数阀双保险。
- 宽限期 3s 是固定常数，**不随时间片增长**——时间片延长的始终是"免罚额度"。
- **与超频正交**：超频缩节拍（TickInterval↓），时间片涨容差（窗口↑），两条独立成长轴；超频流天然更需要时间片。
- ⚠️ 驱动层规则修订：现行"下一 tick = max(固定间隔到点, 程序跑完时刻)"（CODE_EXECUTION.md §3）改为带上限——程序最多跑到"到点 + 时间窗 + 3s 宽限"，宽限耗尽强杀。DemoUI / TickDriver 落地时一并改。

## 2. RAM 与驱动块

- **RAM → 行数上限**：MaxLines 基础 8（IDEA.md §4），RAM 兑换 +1 行/级，成本递增（锚点：第 1 行 3 条，此后 5、8……待实测）。行数是"塞得下"的阀门，周期是"跑得起"的阀门，构成空间/时间双预算。
- **驱动块 → 技能树**：技能树系统未设计（IDEA.md §8 词汇表解锁是其雏形），驱动块先作纯货币记账，用途待技能树设计稿定。

## 3. 搜打撤：局内与局外的管理

三层容器，死亡规则只有一条：

| 容器 | 内容 | 死亡时 | 撤离时 |
|---|---|---|---|
| 局内 Routine | 正在跑的 Build（带入的块 + 局内新装的块） | 全部失去（带入的块按 IDEA.md §6 掉落，备份保险可保 1~2 块） | 随背包一并入库 |
| 局内背包 | 搜到待带出的块与材料 | **全部消失** | 全部进 Codebase |
| 局外 Codebase | 仓库、已兑换成长、合成台 | 不受影响 | — |

- **拾取二选一**（IDEA.md §5 拾取决策的落地）：**装备**——直接进当前 Routine，下个 tick 生效（重载函数）；或**入包**——带走，撤离后处理。材料只能入包（材料不进 Build）。
- **撤离是唯一入库通道**：编译出口读条撤离，局内所得全部落袋；死亡则两层全清，材料也不例外——单一条规则，不搞特例，搜打撤的张力全部来自这里。

### 块合成（局外，Codebase 合成台；实现见 doc/CODEBASE.md）

- **参数化掉落**：带形参的块以"固化形参"形态掉落——attack(1)、attack(2)……数值烧死在块里，不能改。
- **合成规则**：**两块同级 → 一块升一档**（attack(1) + attack(1) → attack(2)）。合成的双重收益：数值升档 + 省 1 行、省 1 次调用周期——与 RAM（行数上限）成长直接联动：前期掉落多而杂，合起来腾行数。
- **attack() 自由形参**：最稀有的一档，形参留空，拼装时可绑定任意变量/表达式（黑板变量、传感器）——随 Build 成长的严格上位。获取途径待定（Boss？深层？）。
- heal(n) / shield(n) 等带参块套用同一阶梯（推广规则，待实测后确认）。

## 4. 掉落表（含主题绑定，定稿）

| 来源 | 掉落 |
|---|---|
| 普通小怪 | 35% 掉 1×主材料 |
| 精英 | 必掉 2：主材料×1 + 随机一种×1 |
| 关底 Boss | 必掉 时间片×5 + RAM×5 + 驱动块×1，另加专属代码语句（首杀，§5） |

- **主题绑定**：每种怪有主材料——时间片←空指针/断点/看门狗/过热；RAM←深拷贝/父进程/僵尸进程；驱动块←Bug/异常/风暴/编译中。完整映射见 EnemyTable.json。
- 拾取：接触拾取（吸附半径表现层做）；块可装备可入包，材料只入包。
- **汇总投放表**：全部怪物的 id / 等级属性 / 出怪积分 / 主材料 / 描述文本在
  `CPPRogue/Assets/Resources/EnemyTable.json`（运行时加载，改表不用重编译；
  代码内置镜像与同步测试见 doc/CODEX.md）；属性与积分的源头仍是 EnemyDesign.md，调参时同步。

## 5. 关底 Boss 与专属代码语句（框架定稿，具体设计后续提供）

掉落侧规则（已定）：

- 每个副本的关底固定一个 Boss。
- Boss 掉落：时间片×5 + RAM×5 + 驱动块×1 + **专属代码语句**。
- **专属代码语句**：全游戏唯一来源的语法块，稀有度在 IDEA.md §5 映射之上再加"专属级"；**首杀必掉**，进局外 Codebase 永久解锁；重复击杀不再掉语句，只掉材料。

设计契约——每个 Boss 必须回答三件事，是"所读即所跑"（IDEA.md §3.4）的自然延伸：

1. 专属语句是什么（语法形态 + 战斗效果）？
2. Boss 自己怎么用它打你（它的屏显源码里就写着这句，玩家先见过才会用）？
3. 玩家拿到后得到什么（Boss 的核心能力易主）？

已预留概念（不得挪用为小怪，EnemyDesign.md §0 / IDEA.md §7）：死循环 `for(;;)`、垃圾回收器 GC、祖传屎山、Kernel Panic（最终 Boss，掉 `main()`）。候选语句池：IDEA.md §3.3（class/模板/指针/inline/lambda……）。

Boss 设计模板（复制填写）：

```
## Boss：中文名（短）+ 英文名（梗）
- 所属副本：___（关底）
- 专属代码语句：___
- 联动设计：___（它怎么用这句 / 玩家怎么反制）
- 属性与数值：hp_ atk_ spd_（EnemyDesign.md §0 等级锚点）
- fun：状态机要点
- 掉落：时间片×5 + RAM×5 + 驱动块×1 + 专属语句（首杀）
- 表现层彩蛋：___
```

## 6. 待定问题

- [ ] 超时惩罚实测：移速衰减是否线性；眩晕期间沿用 0.1s 全局无敌帧规则与否
- [ ] 合成档位数值曲线；attack() 自由形参的获取途径
- [ ] heal(n)/shield(n) 是否套用同一合成阶梯
- [ ] 专属语句死亡是否保留（推荐：按"知识"处理——首杀即永久解锁，死亡不丢；待确认）
- [ ] 备份保险（IDEA.md §6）是否覆盖材料
- [ ] 时间片/RAM 兑换曲线实测（当前锚点：+1s=5 片，+1 行=3 条起）
