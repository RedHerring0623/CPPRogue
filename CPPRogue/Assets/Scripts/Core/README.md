# Core — 逻辑层

命名约定、边界规则、运行方式的正式版本在 **`doc/OVERVIEW.md`**（按功能块拆分的开发文档入口），
各模块的详细文档也在那里：

| 模块 | 文档 |
|---|---|
| `Code/` 代码执行系统 | `doc/CODE_EXECUTION.md` |
| `Combat/` 战斗端口与黑板 | `doc/COMBAT.md` |
| `Computing/` CPU 周期 | `doc/COMPUTING.md` |
| `Physics/` 碰撞判定 | `doc/PHYSICS.md` |

写代码前先看 OVERVIEW 的两节：**§4 命名约定**（一词一义 + 禁用词）、
**§5 逻辑层边界规则**（什么 Unity 类型能进逻辑层）。
