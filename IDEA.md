# CRogue — 用 C++ 语法拼出来的肉鸽游戏

> **一句话概念**：战利品不是剑和盾，而是 `if`、`for`、`while`——玩家把掉落的语法块拼装成一个 C++ 函数，副本里每隔 X 秒自动调用一次，你的代码就是你的 Build，你的 Build 就是你的命运。

---

## 1. 为什么这个点子好（核心卖点）

1. **战利品即语法**。肉鸽的爽点是"捡东西 → Build 变强 → 数值爆炸"，这里每个掉落物都是一段真正的编程能力：捡到 `for` 你就能连击，捡到 `if` 你就有了条件反射，捡到 `try/catch` 你就获得了应急保命。程序员的直觉直接变成游戏理解力。
2. **构筑深度天然存在**。C++ 本身的复杂度（作用域、循环、递归、模板、继承）就是免费的技能树，不用人为发明系统，构筑组合数是天文数字。
3. **语法错误 = 玩法**。漏拼分号、死循环、栈溢出、内存泄漏——别人的游戏里这些是 Bug，你的游戏里它们是机制和笑点，传播性极强（"我死于 while(true)"天然是段子）。
4. **差异化明确**。市面上的编程游戏（Zachtronics 系、Human Resource Machine）都是解谜，没有一个是"编程 + 肉鸽掉落构筑"。缝合的两个母体（编程玩法 × 搜打撤/肉鸽）都验证过，但交叉点无人占据。

---

## 2. 核心玩法循环

```
局内（一次副本/一趟撤离）
  进入副本 → 怪物掉落语法块 → 实时躲避走位
  → 每 X 秒（默认 3s）系统调用你的函数一次，自动执行攻击/防御/回血
  → 死循环/栈溢出会有真实惩罚（见 §4）

局外（仓库 = 你的代码库 Codebase）
  整理/替换语法块 → 重新拼装函数 → 模拟运行测试
  → 用更强的 Build 挑更深楼层 → 解锁更高级的语法词汇表
```

**关键设计一：双手走位，代码出手**。玩家双手只负责**移动走位**（类吸血鬼幸存者/挺进地牢的实时操作），代码负责**每 tick 做什么**。操作给玩家动作游戏的手感，代码给玩家构筑游戏的脑感，两者不冲突。

**关键设计二：执行必须可见**。tick 执行时高亮当前正在运行的语句块，侧栏实时显示变量值（调试器的 watch 窗口）。这一件事同时解决三个问题：爽点（"看着我的 `if` 生效"）、教学（新手靠看执行流学会读代码）、调试（"为什么没生效"一眼可见）。反面预警：没有执行可视化，3 秒 tick 会退化成"普通走位游戏 + 每 3 秒放一次大招"，代码沦为花哨的技能配置器——这是本作最大的设计风险。

---

## 3. 代码 → 战斗的机制映射表（本作的灵魂）

### 3.1 基础语句（普通掉落）

| 语法块 | 战斗效果 |
|---|---|
| `attack();` | 普通攻击，默认行为 |
| `heal(n);` | 回复 n 点生命 |
| `shield(n);` | 获得持续到下个 tick 的护盾 |
| `dash();` | 向移动方向闪避一段 |
| `a = 值;` | 定义变量（怒气、层数、计时器…），变量在 tick 之间持久保存 |
| `if (条件) { }` | 条件触发：`if (hp < 30%) heal(50);` |
| `for (i=0; i<n; i++)` | 连击 n 次，每次伤害衰减 |
| `return;` | 提前结束本 tick，获得短暂无敌帧（"提前返回，懒得打了"）。代价：清空本 tick 已积累的增益/层数——8 行塞 7 个 `return` 刷无敌帧循环不成立 |
| `switch` | 按敌人类型/自身状态分派不同反应 |

### 3.2 危险但强大的结构（稀有掉落，高风险高回报）

| 语法块 | 战斗效果 |
|---|---|
| `while (条件) { }` | 强力引导攻击，但**条件永不满足 = 死循环**：角色当场卡死（挂起），持续受到伤害直到被打或超时 kill 进程。自己保证循环可退出是玩家的责任 |
| `break;` | 立即打断当前引导/循环，并获得打断反击加成 |
| `continue;` | 跳过本 tick 剩余语句，跳过的语句返还部分 CPU 周期 |
| 递归调用自身 | 效果叠层，但层数超过**栈深度**上限 = 栈溢出（Stack Overflow）：全部层数瞬间炸开，自伤+眩晕 |
| `new` / `delete` | `new` 召唤一个持续存在的召唤物；不 `delete` 则积累**内存泄漏** debuff（每层降低全属性）。`delete` 主动回收召唤物并清除泄漏层数 |
| `try { } catch` | catch 块 = 应急预案：被打断/受致命伤时执行 catch 内容（保命神技） |
| `static` 变量 | 局内永久变量（普通变量整局持久、死亡清零；`static` 死亡也不清——局外成长型 Build）。⚠ 与搜打撤"死亡掉块"规则直接冲突，须按模式分别定义（见 §13 待定） |
| `const` | 效果永久固化，不可再修改，但数值 +50% |

### 3.3 后期语法（Boss/深层掉落，版本内容）

- `class` / 继承：把两个函数的能力合并派生出第三个（技能融合系统）
- 模板 `<T>`：泛型强化，同一语句对多属性生效
- 指针/引用：指向敌人 = 锁定标记；指向自己变量 = 属性联动
- `inline`：语句不再占用 tick，改为实时响应（极大提速，代价昂贵）。注意："代码即反应"的体验不能锁在后期——早期以轻量词缀下放（见 §5"实时触发"词缀），`inline` 是它的完全体
- lambda：把一块逻辑"存起来"扔出去，变成一个地雷/无人机

### 3.4 怪物也会扔代码

敌人可以对你 **throw 异常**（中毒、诅咒 = 未捕获的异常在身上跳）；Boss 战是读对方的"源码"找弱点——**Boss 的攻击模式就写在屏幕上，玩家看懂伪代码就看得懂弹幕**。这既是难度也是教学。

---

## 4. 资源与平衡系统（防止"全都堆上"）

| 概念 | 规则 |
|---|---|
| **CPU 周期（蓝条）** | 每条语句每 tick 消耗周期，越重的语句越贵。周期不够 = 语句被"优化掉"（不执行）。这是核心平衡阀门 |
| **函数行数上限** | 初始 8 行，装备/成长扩展。逼玩家做取舍 |
| **栈深度** | 递归/嵌套调用上限，防无限套娃 |
| **Tick 间隔 X** | 基础 3 秒，可被"超频"词缀缩短（攻速概念） |
| **变量槽位** | 初始 2 个，限制状态机的复杂度 |

**表达式分层（拼装 UI 可行性的前提）**：操作数分两类，规则是**传感器预置、玩家只拼控制流和动作**——

- **传感器（预置，所有局固定提供）**：`hp`、敌人数量、最近敌人距离、已叠层数、计时器。条件表达式只能从传感器里选，玩家不收集操作数
- **动作与控制流（掉落物）**：`attack`/`heal`/`if`/`for`/`while`…这些才是构筑素材

不这样切分，要么表达式自由度太低（构筑快速收敛），要么操作数也要收集（块拼块的复杂度爆炸，拖拽 UI 不可用）。

**平衡铁律**：CPU 周期只管语句**数量**，管不住组合的**质变**（无敌帧流、static 跨局成长流）。所以每个高危结构（`while`/递归/`new`/`try-catch`/`static`/`const`）必须自带真实风险，"无风险的高强度结构"禁止存在。周期是阀门，风险是保险丝。

> 工程备注：绝不真的编译玩家代码。实现为一个**沙盒解释器**逐 tick 执行块状 AST，语法合法性由拼装规则保证（块拼不上 = 语法错误，天然防呆）。**架构红线：玩家永远无法输入自由文本**，一切代码只能由块拼装产生——这同时是安全边界（未来创意工坊/代码分享无注入风险）和防呆边界。

---

## 5. 道具与掉落

- **升级材料（局外成长，详见 [LootDesign.md](LootDesign.md)）**：普通/精英小怪掉「时间片 / RAM / 驱动块」（均按数量计），分别延长单次 tick 的执行时间窗（超时三段惩罚：减速→掉血→眩晕，防死循环刷收益）、提升 Routine 行数上限、作技能树货币；关底 Boss 固定掉 时间片×5 + RAM×5 + 驱动块×1，另掉**专属代码语句**（全游戏唯一）
- **块合成（局外，LootDesign.md §3）**：带参块按固化形参掉落（attack(1)/attack(2)…），两块同级合成一块升一档；最稀有的 `attack()` 形参留空，可绑定任意变量——随 Build 成长的严格上位
- **稀有度映射到语法等级**：白（字面量/基础调用）→ 蓝（分支循环）→ 紫（try/catch、static、指针）→ 金（模板、继承）→ 彩蛋级（`goto`：随机传送+随机效果，纯整活）
- **词缀系统**：同一个 `for` 掉落物可以带词缀，如"`for`：迭代次数 +1""`for`：每次暴击""`heal`：实时触发（受致命伤立即执行，不等 tick）"。实时触发类词缀是 `inline` 的早期轻量版，让玩家从第一局就摸到"代码即反应"
- **拾取决策**：副本内实时替换语法块（重载函数，下个 tick 生效）或存背包撤离后处理
- **未鉴定的块**：搜打撤模式里，深层掉落可以是"加密的语法块"，撤离后才解密，赌狗快乐源泉

---

## 6. 两种模式（用户构想的双形态）

### 模式 A：肉鸽爬塔（Roguelike）
- 房间制推进，每层结尾三选一语法块，死亡失去局内所得
- 类似杀戮尖塔结构，但战斗是实时走位 + tick 执行
- 适合做**主线体验和教学**

### 模式 B：搜打撤（Extraction，重点形态）
- 世界观完美自洽：你是潜入**废弃主机**回收源代码片段的回收程序
- **仓库 = 你的持久代码库**，带进副本的块死亡会掉落（可买"备份保险"保住 1~2 块）
- 撤离点 = **编译出口**，读条撤离 = `g++ compiling...`
- 越深层语法越古老越强大（lambda、concept、协程藏在最底层）
- 阵营声望：不同"编译器派系"（GCC 部族 / Clang 游侠 / MSVC 帝国的笑话随便玩）
- 玩家之间的"代码库"就是身份象征，截图分享欲极强

**模式取舍结论：押注搜打撤**。理由：① 世界观、传播素材、身份系统（代码库 = 玩家身份）全在搜打撤侧；② 爬塔的优点只是"结构简单"，不是魅力所在；③ 小团队养两个核心循环非常危险。爬塔降级为搜打撤副本内的**深层推进结构**（楼层制），不再作为并列模式。MVP 原型则两者都不做，用无模式的纯房间战斗验证 tick 玩法（见 §12）。

---

## 7. 怪物与 Boss（全程序文化梗）

| 怪物 | 设计 |
|---|---|
| Bug 虫群 | 基础小怪，成群刷新 |
| 野指针 | 刺客型，闪烁瞬移，指向随机位置乱撞 |
| 死锁 Deadlock | 双子怪，互锁时无敌，需同时击杀 |
| 竞态条件 | 会"抢跑"你的函数：推迟你本 tick 的执行 |
| 内存泄漏 | 越打越大的史莱姆，不杀会涨到塞满屏幕 |
| **Boss：垃圾回收器 GC** | 场景定期"GC 扫描"，把你 `static` 的增益全清掉 |
| **Boss：祖传屎山** | 由无数嵌套 `if` 组成的巨型怪物，剥开嵌套层才能打到核心 |
| **最终 Boss：内核恐慌 Kernel Panic** | 全屏弹幕即"内核转储"，掉落通关彩蛋语法 `main()` |

> 设计纪律：每个怪物先作为**机制**成立，再套梗名字——梗是给懂梗的人的糖，不能让梗决定设计。
> 关底 Boss 的掉落规则、专属代码语句契约与设计模板见 [LootDesign.md](LootDesign.md) §4。

---

## 8. 局外成长

- **词汇表解锁**：初期只开放顺序/分支/循环，等级提升解锁 OOP、模板、异常——相当于天赋树就是 C++ 教学大纲
- **角色 = 不同"编译器/架构"**：开局函数签名不同。例：快速但行数少的"轻量脚本引擎"、周期多但 tick 慢的"批处理器"、开局自带 `try/catch` 的"稳健老编译器"
- 隐藏成就全用编译器梗：`Hello, World`、`-O2 优化`、`undef` 行为……

---

## 9. 世界观（一句话包装）

> 一台濒死的老主机，系统被 Bug 腐蚀殆尽。你是最后一段还在运行的自举程序，深入内存的废墟，回收失落的语法，把自己一路编译成更高级的存在——直到有能力重写内核。

---

## 10. 风险与对策

| 风险 | 对策 |
|---|---|
| 非程序员玩不懂 | 真正的门槛不是看不懂 `if`，而是**调试心智**——"为什么我的代码没生效"需要会读执行流。对策：① 执行可视化（语句高亮 + watch 窗口）是生命线，列为垂直切片第一优先级；② 拼块制+物理防呆，拼得上就能跑；③ 每个块背面写大白话效果（"`if` = 血少时才回血"）；④ 预设"抄作业"系统，可复制 Boss/NPC 的现成函数；⑤ 目标客群本来就是程序员和极客，社区梗传播是主要获客手段 |
| C++ 太硬核 | 实际是"C++ 风味的 DSL"，砍掉真实 C++ 的坑（头文件、模板错误信息），保留戏剧性强的部分（指针、内存、异常） |
| 组合爆炸难平衡 | CPU 周期成本系统是总阀门：任何超模组合最终都卡在"周期不够"，数值可控 |
| 死循环劝退 | 死循环有保底：超时自动 kill -9，只惩罚不致命；并把它做成可玩的赌博流派（就是有人爱玩心跳） |

---

## 11. 参考对标

- 构筑爽感：杀戮尖塔 / 吸血鬼幸存者
- 编程玩法：EXAPUNKS、7 Billion Humans、while True: learn()
- 撤离循环：逃离塔科夫 / 暗区突围
- 代码叙事彩蛋：else Heart.Break()
- **无人占据的交叉点**：编程 × 肉鸽掉落构筑

---

## 12. MVP 路线图（建议）

1. **原型（2~4 周）**：灰盒 demo，仅 6 种块（调用/if/for/变量/return/while），1 张地图，验证"每 3 秒执行我的代码"是否好玩——这是唯一的生死命题。第 7 项交付物是**执行可视化**（当前语句高亮 + 变量 watch 窗口）：它不是装饰，是验证核心假设的观测手段，没有它你看不到"不好玩"的原因出在哪
2. **垂直切片**：**执行可视化打磨到爽**（第一优先级）、完整编辑器 UX（拖拽拼装+防呆+传感器面板）、CPU 周期系统、5 种怪 + 1 个 Boss、掉落稀有度
3. **模式验证**：直接做搜打撤单局循环（进入 → 捡块 → 拼装 → 撤离），爬塔结构降级为副本内楼层推进；仓库/保险/撤离按此顺序拆解
4. **EA 上线**：程序员社区（V2EX / Reddit / B 站）冷启动，"你的死因是 segfault"是天然传播素材

**技术选型建议**：Godot 或 Unity（解释器自己写，几百行就能跑块状 AST）；有执念可以用 C++ 自研，但原型阶段不建议。

---

## 13. 待定问题（下次讨论）

- [ ] tick 间隔固定 3s 还是随 Build 可变？超频流是否会滚雪球
- [ ] `static` 在搜打撤模式死亡时如何处理：清空 / 随块掉落 / 保险覆盖？
- [ ] 搜打撤和肉鸽做在同一个产品里，还是先做哪个（§6 已有倾向结论：押注搜打撤，待最终确认）
- [ ] 多人愿景：合作 = 多线程（两人函数并行执行），PvP = 竞态条件，放多远
- [ ] 移动端适配（块状拖拽其实非常适合触屏）
